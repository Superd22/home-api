# Launch HomeAPI main server
[Unit]
Description=HOMEAPI
[Service]
TimeoutStartSec=0
ExecStart=-/usr/bin/docker run -v /etc/openvpn/client/:/volumes/openvpn/:rw -e NODE_ENV=prod -p 3000:3000
[Install]
WantedBy=multi-user.target